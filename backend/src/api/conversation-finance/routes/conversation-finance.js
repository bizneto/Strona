'use strict';

/**
 * conversation-finance router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::conversation-finance.conversation-finance');
