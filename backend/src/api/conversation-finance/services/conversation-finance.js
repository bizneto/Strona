'use strict';

/**
 * conversation-finance service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::conversation-finance.conversation-finance');
